use StdIO: all;
// use Array: all;
// use BlasLevel1: all;
// use BlasLevel2: all;
// use Helper: all;
use Math: all;
use Structures: all;

// This breaks with -check c
int main() {
    a = [toc(1,1)];
    b = toc(1,1);
    print(a[0] * b);
    return 0;
}
