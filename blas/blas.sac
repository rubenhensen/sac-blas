use StdIO: all;
use Array: all;
use BlasLevel1: all;

int main() {
    X = tod(iota(10));
    Y = tod(iota(10)) + 10.0;
    print(X);
    print(Y);

    Xp, Yp = dswap(10, X, 1, Y, 1);
    print(Xp);
    print(Yp);

    Xp, Yp = dswap(5, X, 2, Y, 2);
    print(Xp);
    print(Yp);

    return 0;
}
